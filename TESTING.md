# Testing

## 原则

开发和破坏性测试只在本地模拟目录进行。不得使用生产世界测试恢复、覆盖、模组删除、主版本升级或失败回滚。

## 测试环境

`tests/fixtures/mock-server/` 提供最小日志和模组数据。mock adapter 在内存中模拟服务状态、玩家、指标、备份和操作结果；helper 测试使用 pytest 临时目录及 fake runner，不访问真实世界。

集成测试使用临时目录、临时 SQLite 和无网络 fake AI provider。Playwright 使用独立 mock 进程、临时数据库和测试账号，不读取生产 profile。真实 AI 测试必须显式启用、限制为少量请求，并使用轮换后的测试密钥。

首次运行浏览器验收前执行 `npm --prefix apps/web exec -- playwright install chromium`，之后使用 `npm --prefix apps/web run test:e2e`。CI 会安装 Chromium 及必要的 Linux 系统依赖。

## 单元测试

- 配置解析、范围和额外字段拒绝。
- 路径穿越、软链接、控制字符和命令注入样本。
- Minecraft/Forge/Fabric/NeoForge/Quilt 元数据识别。
- 日志截断、去重和敏感信息脱敏。
- 密码哈希、会话过期、CSRF 和角色权限。
- 风险分级、确认过期、参数哈希和防重放。
- AI 限流、每日额度、超时和 provider 失败降级。
- SQLite 初始化、操作状态和审计写入。

## Helper 测试

- 所有未知 operation 必须拒绝。
- JSON 超限、重复键、错误类型和额外字段必须拒绝。
- 不继承危险环境变量，不调用 Shell。
- subprocess argv、超时、输出上限和退出码分类正确。
- 只允许固定 service、screen 和路径。
- 写入保留所有者/模式并原子替换。
- 备份归档拒绝绝对路径、`..`、设备文件和危险软链接。
- restore 任一步失败都会保留 recovery 目录并尝试恢复服务。
- 备份凭据在归档替换、内容变更、软链接或不可信目录下必须失效。

## API 集成测试

- 未登录 API 和 WebSocket 全部拒绝。
- 登录、退出撤销、CSRF、Origin 和 WebSocket ticket。
- 中风险无确认不执行，高风险只确认一次不执行。
- AI 关闭或 provider 失败时传统功能正常。
- 服务器 fingerprint 变化后写工具全部锁定。

## UI 验收

- 首页首屏只显示总体状态、玩家、通俗资源状态、主要动作和 AI 输入。
- 高级工具默认折叠。
- 所有动作显示用途、影响、风险、停服和恢复说明。
- 已在真实浏览器检查 320px、390px、768px 和桌面布局，无横向溢出或文字容器溢出。
- 深色/亮色对比度、键盘导航、焦点、错误和加载状态完整。
- 页面刷新后会话、主题和操作进度正确恢复。
- 手机导航抽屉、主题切换和中风险确认弹窗已完成交互验收；取消后服务状态不变。
- 浏览器控制台无 warning/error；公开截图只使用 mock 服务器和虚构玩家。
- Playwright CI 实际覆盖登录后刷新、中风险取消、高风险二次确认、备份完整校验、移动导航、主题持久化和退出。

## 0.4.1 Alpha 基线

- 后端：137 个 pytest 测试通过，Ruff 与 strict mypy 通过，覆盖率 79%。
- 前端：12 个 Vitest 文件、24 个测试通过，2 个 Playwright Chromium 场景通过，TypeScript/Vite 构建通过。
- 运行依赖：`npm audit --omit=dev` 为 0 个已知漏洞。
- 部署资源：全部 Shell 脚本通过 `bash -n`，运行 profile 的权限、拒绝覆盖和密钥不回显均有自动测试。

## 安全测试样本

- `../../world`、URL 编码穿越、Unicode 分隔符和软链接逃逸。
- `; reboot`、换行命令、反引号、`$()` 和空字节。
- 日志内提示注入、伪造 tool call 和超长堆栈。
- HTML/JavaScript、危险 Markdown URL 和终端 ANSI 序列。
- API key、Authorization、Cookie、密码、IPv4/IPv6 和私钥脱敏。
- 并发重启/备份/恢复和确认 token 重放。

## 模拟验收矩阵

| 功能 | 模拟环境 | 生产只读 | 生产写入 |
|---|---:|---:|---:|
| 登录、会话、审计 | 必须 | 可验 | 不涉及 Minecraft |
| 状态、玩家、日志、指标 | 必须 | 必须 | 不涉及 |
| 控制台公告 | 必须 | 不执行 | 维护窗口验收 |
| 启停和重启 | 必须 | 不执行 | 维护窗口验收 |
| 手动备份 | 必须 | 查看现有记录 | 维护窗口验收 |
| restore 成功/失败回滚 | 必须 | 不执行 | 禁止在真实世界演练 |
| properties/白名单/OP | 必须 | 只读 | 使用可逆测试项验收 |
| 模组安装/停用 | 必须 | 只读元数据 | 默认不在首轮生产验收 |
| AI 对话和工具提议 | fake provider 必须 | 可关闭 | 新密钥后少量验收 |

## 阶段完成门槛

阶段 3：类型检查、格式检查和单元测试通过；无 secret 扫描命中。

阶段 4：模拟集成、权限、注入、恢复失败和移动端测试通过；不连接生产写接口。

阶段 5：再次列出全部用户、文件、服务、端口、软件包、sudoers 和可能中断，并获得明确授权。

阶段 6：逐项记录结果、证据、未执行项和已知限制。面板崩溃测试不得影响 Minecraft。
