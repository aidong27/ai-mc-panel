# Recovery

## 恢复目标

任何面板失败都不得拖垮 Minecraft。任何改变 Minecraft 数据的动作都必须能识别失败、保留当前状态并提供人工接管路径。

## 面板服务故障

面板异常时：

1. 停止 `mc-panel.service`。
2. 验证 `minecraft.service`、Java 进程和 25565 端口仍正常。
3. 查看面板日志和最近审计事件。
4. 回退 `/opt/mc-panel/current` 到上一 release。
5. 必要时恢复迁移前 SQLite 副本。

整个过程不停止 Minecraft。

## 公网入口回滚

反向代理、证书和防火墙不由面板安装器管理。公网入口异常时，先停止或回退反向代理，保留面板 API 的回环监听，然后验证 Minecraft service、PID、重启计数和游戏端口未变。不要通过临时公开 `18080` 绕过 HTTPS。

网络回滚不应删除证书、数据库、审计日志或旧 release；待原因查明后再人工决定是否清理。

## 发布回滚

每次更新保留前一个 release，并用 SQLite backup API 创建一致副本和 SHA256。新版本通过健康检查前不删除旧版本；启动失败会保留失败数据库文件并恢复副本。手动回退旧 release 默认先关闭生产写权限。

更新恢复点保存在 `/var/backups/mc-panel/update-<timestamp>/`。其中包含前一 release、迁移前 SQLite 一致副本、SHA256、helper、systemd unit 和运行路径 drop-in。回滚脚本恢复对应 release 与数据库，不停止 Minecraft；具体目标版本必须由管理员显式输入。

## 配置修改恢复点

- 修改 `server.properties` 前复制原文件到 root 专用事件目录，写入使用同目录临时文件和原子替换，并保留原权限与所有者。
- 修改定时备份前保存两个 systemd drop-in；任一步失败时恢复原文件、重新加载 systemd 并重启 timer。恢复无法验证时返回 `rollback_failed` 和恢复点路径。
- 白名单和 OP 使用可逆的 Minecraft 控制台命令，不创建文件恢复点；相反操作及结果都写入审计。
- 模组安装记录隔离区元数据和“目标原本不存在”，停用模组则把原 JAR 移到唯一保留目录。

所有写操作执行前仍会核对服务器 fingerprint。恢复点不等于已经完成业务验证，界面必须以 helper 的具体结果为准。

## 模组操作

- 上传先进入面板隔离区，不直接进入 `mods/`。
- 安装前解析 JAR ZIP 结构和加载器元数据，生成 SHA256。
- “移除”实际移动到 `mods-disabled/panel-removed/<event-id>/`，不删除。
- 批量操作属于高风险，MVP 默认禁用。
- 安装或停用只落盘并返回“需要重启”，不会自动重启生产服。
- MVP 不会在重启失败后自动猜测并回滚模组；管理员应根据返回的恢复点人工恢复，真实模组变更须另行安排维护窗口。

## 世界备份

生产接入要求现有备份流程能够创建世界、模组和关键配置的一致副本，并生成独立 checksum sidecar。面板复用已审计的备份命令，但不得直接把通用校验脚本或任意参数暴露给服务用户。

备份完成标准：现有备份命令成功、SHA256 文件生成且匹配、归档成员路径安全、必需条目存在、`minecraft.service` 活跃且 25565 可连接。

## 恢复备份协议

恢复属于高风险，必须在维护窗口执行：

1. 重新检测服务器 fingerprint、磁盘和在线玩家。
2. 显示备份时间、大小、将覆盖的对象和预计停服时间；首版不从归档推断世界版本。
3. 第一次确认选择备份；第二次确认输入服务器名称。
4. 自动创建当前状态冷备份并完成校验。
5. 验证目标归档 SHA256、必需目录、路径安全和剩余空间。
6. 停止 Minecraft，确认 Java 进程和游戏端口退出。
7. 解压到同一文件系统的 root 所有 staging 目录，禁止直接覆盖。
8. 把当前目录原子移动到 recovery 目录，再把 staging 内容就位。
9. 恢复所有者和模式，启动 Minecraft。
10. 验证 systemd 与 25565 端口恢复；版本和错误日志由恢复后的只读状态页继续核对。
11. 启动验证失败时停止服务，把新数据移到 `failed-new-data`，原子移回 recovery 内容并尝试重新启动原状态。

任何步骤失败都不得删除目标备份、恢复点或旧世界目录。若回滚后的原服务也未恢复，helper 会保留两套数据并返回失败，必须人工接管，不能宣称恢复成功。

## 当前限制

- 不在生产世界执行恢复演练。
- 模拟环境只验证流程、路径安全、失败回滚和状态机。
- 不提供删除世界功能。
- 不自动升级 Minecraft、Forge、Java 或大型模组包。
- 没有“失败后自动永久锁死所有写工具”的数据库闩锁；出现恢复或回滚异常时，运维流程必须立即运行 `disable-writes.sh`，保留现场后人工检查。
- 现有归档不包含 libraries、versions、日志和崩溃报告，因此不能单独重建整台主机；完整灾备还需要独立的应用清单和安装包归档。

## 紧急人工接管

helper 返回 `rollback_failed`、`verification_failed` 或恢复操作失败时，先运行：

```bash
sudo /opt/mc-panel/current/deploy/scripts/disable-writes.sh
```

随后核对 `minecraft.service`、25565、恢复点和审计事件。AI 只能解释现状，不得继续提出连续写操作；MVP 不提供绕过确认的“自动修复”。
